package virtualPlans.AccProject;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = "virtualPlans.AccProject")
public class AccProjectApplication {
    public static void main(String[] args) {
        SpringApplication.run(AccProjectApplication.class, args);
    }

}
